// This project uses Tailwind v4 — configuration lives in src/styles.css
// (see @theme inline). This stub silences a build helper that scans for a
// classic config file. Do not move design tokens here.
import type { Config } from "tailwindcss";
const config: Config = { content: ["./src/**/*.{ts,tsx}"], theme: { extend: {} }, plugins: [] };
export default config;
